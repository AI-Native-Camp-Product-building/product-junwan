# 탐색 차트 드릴다운 분리 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 탐색 페이지에서 dim을 추가해도 차트는 "primary dim 1개"만 시리즈화하여 안 쪼개지게 하고, 표는 전체 dim 그대로 보여주는 드릴다운 패턴 구현.

**Architecture:**

1. `ExploreChart`에 `primaryDim` 상태 추가 — 차트 헤더에 드롭다운으로 노출.
2. Chart self-fetch 시 `chartDimensions = [timeDim, primaryDim]` 만 사용 → 작품명 등 추가 dim이 들어와도 차트 시리즈는 primary 기준으로 유지.
3. `query-result-table`는 기존 구조 유지 — 전체 dim으로 표시.
4. Primary dim 카디널리티 폭발 대비 top-N 제한 (기본 10, 나머지는 "기타"로 묶음).
5. 기존 메인 query / 집계 경로(`query-engine.ts`) 변경 없음.

**Tech Stack:** Next.js 16.2.2 (App Router, Turbopack) · TypeScript strict · shadcn/ui (base-ui) · Recharts 3.8 · Tailwind CSS v4 · Vitest.

---

## File Structure

**수정:**

- `src/components/explore/explore-chart.tsx` — primary dim 상태, selector UI, self-fetch dim 축약, top-N rollup.
- `src/app/dashboard/explore/page.tsx` — 필요 시 `ExploreChart` 호출부 정리 (props 변화 없다면 수정 없음).

**신규 없음.**

**테스트:**

- `src/components/explore/__tests__/explore-chart-primary-dim.test.ts` (또는 `src/lib/__tests__/rollup-top-n.test.ts`) — top-N rollup 로직 유닛테스트 1개.

> **참고:** 현재 ExploreChart 내부 rollup 헬퍼(`buildNormalChartData`)는 이미 `seriesDims` 배열을 받음. 이번 작업은 `seriesDims = [primaryDim]` 1개 원소로 제한하고, cardinality가 클 때 top-N 집계 + "기타" 묶는 레이어를 `buildNormalChartData` 전에 추가한다.

---

## Coding Rules 체크리스트

- `space-y-*` / `space-x-*` 금지 → `flex gap-*` 사용.
- raw color 금지 → semantic token (`bg-muted`, `text-muted-foreground`).
- 모든 hooks는 early return 위 배치.
- 인라인 객체 prop 전달 금지 → `useMemo`.
- `any` 금지, 타입 명시.
- Derived 상태는 `useMemo`, `useEffect` 안에서 setState로 계산 금지.

---

### Task 1: Primary dim 상태와 selector UI 추가

**목적:** 차트 헤더에서 사용자가 "어떤 dim으로 시리즈를 나눌지" 선택 가능하게 함. 선택지는 현재 query의 non-time dim 중 하나.

**Files:**

- Modify: `src/components/explore/explore-chart.tsx` (상단 types + 컴포넌트 body)

- [ ] **Step 1: 컴포넌트 상단 types 블록 확인**

`src/components/explore/explore-chart.tsx` 44~62줄의 `ExploreChartProps` 확인. 변경 없음 — primary dim은 내부 상태로만 관리 (URL/부모 상태 동기화는 Task 5에서 선택사항).

- [ ] **Step 2: 컴포넌트 바디 내 primary dim 상태 추가**

기존 코드 (287줄 근처, granularity state 근처):

```tsx
const [granularity, setGranularity] = React.useState<Granularity>("daily");
```

바로 아래에 추가:

```tsx
const [primaryDimOverride, setPrimaryDimOverride] = React.useState<
  string | null
>(null);
```

- [ ] **Step 3: seriesDims 계산 로직 수정 → effectivePrimaryDim 도출**

기존 (288줄 근처):

```tsx
const existingTimeDim = dimensions.find(
  (d) => d === "date" || d === "month" || d === "week",
);
const seriesDims = dimensions.filter(
  (d) => d !== "date" && d !== "month" && d !== "week",
);
const hasCountryDim = dimensions.includes("country");
```

수정:

```tsx
const existingTimeDim = dimensions.find(
  (d) => d === "date" || d === "month" || d === "week",
);
const nonTimeDims = React.useMemo(
  () => dimensions.filter((d) => d !== "date" && d !== "month" && d !== "week"),
  [dimensions],
);
const effectivePrimaryDim = React.useMemo(() => {
  if (primaryDimOverride && nonTimeDims.includes(primaryDimOverride)) {
    return primaryDimOverride;
  }
  return nonTimeDims[0] ?? null;
}, [primaryDimOverride, nonTimeDims]);
const seriesDims = effectivePrimaryDim ? [effectivePrimaryDim] : [];
const hasCountryDim = effectivePrimaryDim === "country";
```

이유: `seriesDims`가 항상 0개 또는 1개만 갖게 됨 → 차트 시리즈가 단일 dim 기준으로만 쪼개짐.

- [ ] **Step 4: Chart 헤더에 primary dim Select 렌더**

파일 뒤쪽 `<CardHeader>` 안 granularity 토글 근처 (현재 메트릭 Select 렌더하는 블록 부근)를 찾아, granularity toggle 옆 또는 아래에 다음을 추가:

```tsx
{
  nonTimeDims.length >= 1 && (
    <Select
      value={effectivePrimaryDim ?? ""}
      onValueChange={(v) => setPrimaryDimOverride(v)}
    >
      <SelectTrigger className="h-7 w-auto min-w-[120px] text-xs">
        <SelectValue placeholder="기준 차원" />
      </SelectTrigger>
      <SelectContent>
        {nonTimeDims.map((d) => (
          <SelectItem key={d} value={d}>
            {String(d)}
          </SelectItem>
        ))}
      </SelectContent>
    </Select>
  );
}
```

> 차원 라벨을 한국어로 표시하려면 `@/config/query-schema`에서 `DIMENSION_MAP.get(d)?.label ?? d`로 치환. 이 레포에 이미 존재하는 패턴 확인 후 사용.

- [ ] **Step 5: 빌드/타입체크**

Run:

```bash
npx tsc --noEmit
```

Expected: 에러 0.

- [ ] **Step 6: dev 서버에서 육안 확인**

Run:

```bash
npm run dev
```

접속: `http://localhost:1004/dashboard/explore`
쿼리: "레진KR + 봄툰 4월 ROAS 추이" 실행 후 작품명 dim 추가 → 차트 헤더에 "기준 차원" Select 노출 확인. country ↔ creative_name 전환 시 시리즈가 바뀌어야 함.

- [ ] **Step 7: Commit**

```bash
git add src/components/explore/explore-chart.tsx
git commit -m "feat(explore): add primary dim selector on chart header"
```

---

### Task 2: Self-fetch 시 차트용 dim을 [timeDim, primaryDim]만 전송

**목적:** 표에 creative_name이 추가되어도 차트 self-fetch는 primary dim만 포함 → 서버 집계 세분화 최소화 + 시리즈 폭발 방지.

**Files:**

- Modify: `src/components/explore/explore-chart.tsx` (Self-fetch useEffect 블록, 298~340줄 부근)

- [ ] **Step 1: Self-fetch query 구성 수정**

기존:

```tsx
const chartDimensions = [timeDimKey, ...seriesDims] as DimensionKey[];
```

이미 Task 1에서 `seriesDims`가 `[primaryDim]` 1개로 축약되므로 **코드 변경 불필요**. 단, 주석으로 의도 명시:

```tsx
// seriesDims는 Task 1에서 primary dim 1개로 축약됨.
// 작품명 등 추가 dim은 표에만 존재, 차트엔 불포함.
const chartDimensions = [timeDimKey, ...seriesDims] as DimensionKey[];
```

- [ ] **Step 2: useEffect 의존성 배열 검증**

기존 (340줄 근처):

```tsx
}, [needsSelfFetch, granularity, metrics, filters, dateRange, seriesDims.join(",")]);
```

`seriesDims.join(",")`가 primary dim 변경 시 다시 fetch되는지 확인. Task 1에서 `seriesDims = [effectivePrimaryDim]`이므로 primaryDimOverride 변경 → seriesDims 배열 변경 → 의존성 변경 → 재fetch됨. 추가 변경 없음.

- [ ] **Step 3: 타입체크 + 육안 확인**

```bash
npx tsc --noEmit
```

dev에서 primary dim 바꿀 때 Network 탭에 `/api/query` 재요청 뜨는지, 응답 rows의 컬럼이 `[date, <primary dim>, <metrics...>]` 구성인지 확인.

- [ ] **Step 4: Commit**

```bash
git add src/components/explore/explore-chart.tsx
git commit -m "refactor(explore): keep chart self-fetch scoped to [time, primary] dims"
```

---

### Task 3: Top-N 롤업 + "기타" 묶기

**목적:** primary dim이 creative_name처럼 카디널리티 높을 때 라인 수백 개 방지. 기본 top 10, 나머지 합산하여 "기타" 시리즈로.

**Files:**

- Modify: `src/components/explore/explore-chart.tsx`
- Create: `src/components/explore/__tests__/rollup-top-n.test.ts`

- [ ] **Step 1: 롤업 헬퍼 함수 추가**

`explore-chart.tsx` 내 `buildNormalChartData` 함수 위쪽(140줄 근처)에 추가:

```tsx
/**
 * primary dim 카디널리티가 limit 초과면 상위 N개만 남기고 나머지는 "기타"로 합산.
 * 선택 기준: 전체 기간 metric 합계 내림차순.
 */
function collapseToTopN(
  rows: QueryResultRow[],
  seriesDim: string,
  metricKey: string,
  limit: number,
): QueryResultRow[] {
  if (!seriesDim) return rows;

  const totals = new Map<string, number>();
  for (const row of rows) {
    const key = String(row[seriesDim] ?? "");
    const v = Number(row[metricKey] ?? 0);
    if (isNaN(v)) continue;
    totals.set(key, (totals.get(key) ?? 0) + v);
  }

  if (totals.size <= limit) return rows;

  const topKeys = new Set(
    [...totals.entries()]
      .sort((a, b) => b[1] - a[1])
      .slice(0, limit)
      .map(([k]) => k),
  );

  return rows.map((row) => {
    const key = String(row[seriesDim] ?? "");
    if (topKeys.has(key)) return row;
    return { ...row, [seriesDim]: "기타" };
  });
}
```

- [ ] **Step 2: chartData 계산 useMemo 안에서 collapseToTopN 적용**

기존 (380줄 근처):

```tsx
const { data, series } = buildNormalChartData(
  effectiveRows,
  effectiveTimeDim,
  seriesDims,
  selectedMetric,
);
```

수정:

```tsx
const rowsForChart =
  seriesDims.length > 0
    ? collapseToTopN(effectiveRows, seriesDims[0], selectedMetric, 10)
    : effectiveRows;

const { data, series } = buildNormalChartData(
  rowsForChart,
  effectiveTimeDim,
  seriesDims,
  selectedMetric,
);
```

- [ ] **Step 3: 실패 테스트 작성 (TDD)**

Create `src/components/explore/__tests__/rollup-top-n.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { collapseToTopN } from "../explore-chart";

describe("collapseToTopN", () => {
  it("keeps rows unchanged when cardinality <= limit", () => {
    const rows = [
      { creative_name: "A", date: "2026-04-01", revenue_krw: 100 },
      { creative_name: "B", date: "2026-04-01", revenue_krw: 50 },
    ];
    const out = collapseToTopN(rows, "creative_name", "revenue_krw", 10);
    expect(out).toHaveLength(2);
    expect(out.every((r) => r.creative_name !== "기타")).toBe(true);
  });

  it("collapses bottom rows to '기타' when cardinality > limit", () => {
    const rows = [
      { creative_name: "A", date: "2026-04-01", revenue_krw: 1000 },
      { creative_name: "B", date: "2026-04-01", revenue_krw: 500 },
      { creative_name: "C", date: "2026-04-01", revenue_krw: 10 },
      { creative_name: "D", date: "2026-04-01", revenue_krw: 5 },
    ];
    const out = collapseToTopN(rows, "creative_name", "revenue_krw", 2);
    const names = out.map((r) => r.creative_name);
    expect(names).toContain("A");
    expect(names).toContain("B");
    expect(names.filter((n) => n === "기타")).toHaveLength(2);
  });
});
```

`collapseToTopN`를 테스트에서 import 가능하도록 `explore-chart.tsx`에서 **named export**로 바꿔야 함:

```tsx
export function collapseToTopN(...) { ... }
```

- [ ] **Step 4: 테스트 실행 — 통과 확인**

```bash
npx vitest run src/components/explore/__tests__/rollup-top-n.test.ts
```

Expected: PASS 2개.

- [ ] **Step 5: 타입체크 + dev 육안 확인**

```bash
npx tsc --noEmit
```

dev에서 creative_name을 primary dim으로 선택 시 시리즈 수가 최대 11개 (top 10 + 기타) 이하인지 확인.

- [ ] **Step 6: Commit**

```bash
git add src/components/explore/explore-chart.tsx src/components/explore/__tests__/rollup-top-n.test.ts
git commit -m "feat(explore): cap chart series to top-N with '기타' fallback"
```

---

### Task 4: Primary dim 리셋 — dimensions 배열이 변하면 override 초기화

**목적:** 사용자가 dim을 제거/교체했을 때 stale한 override가 남지 않게.

**Files:**

- Modify: `src/components/explore/explore-chart.tsx`

- [ ] **Step 1: useEffect로 동기화 추가**

primaryDimOverride 선언 바로 아래에:

```tsx
React.useEffect(() => {
  if (primaryDimOverride && !nonTimeDims.includes(primaryDimOverride)) {
    setPrimaryDimOverride(null);
  }
}, [nonTimeDims, primaryDimOverride]);
```

- [ ] **Step 2: 타입체크**

```bash
npx tsc --noEmit
```

- [ ] **Step 3: 육안 확인**

dev에서:

1. 쿼리 실행 → primary dim에 creative_name 선택.
2. creative_name dim 제거 (쿼리 sentence에서).
3. 차트가 country를 primary로 자동 복귀하는지 확인.

- [ ] **Step 4: Commit**

```bash
git add src/components/explore/explore-chart.tsx
git commit -m "fix(explore): reset chart primary dim when removed from query"
```

---

### Task 5: 문서 업데이트

**목적:** 동작 변경을 CLAUDE.md와 design doc에 기록.

**Files:**

- Modify: `CLAUDE.md` (Current Status 섹션)

- [ ] **Step 1: Current Status 항목 추가**

`CLAUDE.md`의 "### 완료" 체크리스트에 추가:

```md
- [x] 탐색 차트 primary dim 드릴다운: 표는 전체 dim, 차트는 primary dim 1개로 시리즈화 + top-10 "기타" 묶음
```

- [ ] **Step 2: Commit**

```bash
git add CLAUDE.md
git commit -m "docs: note explore chart primary-dim drilldown behavior"
```

---

## Self-Review 체크

- [x] 스펙 커버리지: "표는 전체 dim + 차트는 primary 1개" → Task 1~2, "top-N" → Task 3, "dim 제거시 복구" → Task 4. 모두 태스크에 매핑.
- [x] placeholder 없음 — 모든 step에 실제 코드/명령.
- [x] 타입 일관성: `primaryDimOverride`, `effectivePrimaryDim`, `nonTimeDims`, `collapseToTopN` — Task 1~4 전반에 걸쳐 이름 동일.
- [x] TDD: Task 3에서 테스트 작성 먼저 → 통과 확인 → 커밋.
- [x] DRY/YAGNI: URL 동기화, compare 모드 primary dim override 등은 이번 scope에서 제외 (필요 시 후속 plan).

---

## 후속 작업 (이번 plan 제외)

- Primary dim 선택값을 URL `?primary=creative_name`로 직렬화 (저장 쿼리/공유 링크용).
- Compare 모드에서 primary dim 분리 동작 (현재 compare는 base/compare 2 시리즈 고정).
- Primary dim 카디널리티 시각 경고 (e.g. "25개 작품 중 top 10 표시 중").
- 표 row 클릭 → 해당 dim 값으로 primary 자동 전환 ("이 행 추이 보기" 액션).
